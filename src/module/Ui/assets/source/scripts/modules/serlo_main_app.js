/*global define*/
define(['jquery'], function ($) {
    "use strict";

    function init() {
        // voidvoid
        $.extend({});
    }

    return {
        initialize: function () {
            init();
        }
    };
});