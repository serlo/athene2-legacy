{{ pageHeader('Discussions').setSubtitle(forum ? forum.getName() : '').render() }}
{% include 'discussion/discussions/tree' %}
{% if forum %}
    {% include 'discussion/discussions/discussions' %}
{% endif %}