{% if isGranted('event.log.get', entity) %}
    <li>
        <a href="{{ url('event/history', {'id': entity.getId()}) }}">
            <span class="glyphicon glyphicon-hdd"></span>
            {% trans %} Show logfile {% endtrans %}
        </a>
    </li>
{% endif %}