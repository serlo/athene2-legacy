{% if isGranted('flag.create', entity) %}
    <li>
        <a href="{{ url('flag/add', {'id': entity.getId()}) }}">
            <span class="glyphicon glyphicon-flag"></span>
            {{ translate('Flag content') }}
        </a>
    </li>
{% endif %}