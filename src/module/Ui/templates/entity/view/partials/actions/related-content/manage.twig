{% if isGranted('related_content.manage', entity) %}
    <li>
        <a href="{{ url('related-content/manage', entity.getId()) }}">
            <span class="glyphicon glyphicon-link"></span>
            {{ translate('Manage related items') }}
        </a>
    </li>
{% endif %}