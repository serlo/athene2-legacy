{% if isGranted('entity.trash', entity) %}
    <li>
        <a href="{{ url('uuid/trash', {'id': entity.getId()}) }}">
            <span class="glyphicon glyphicon-trash"></span>
            {{ translate('Move to trash') }}
        </a>
    </li>
{% endif %}