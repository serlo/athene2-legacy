<article>
    <section class="row">
        <div class="col-md-8">
            {{ markdown().toHtml(entity.getCurrentRevision().get('content')) }}
        </div>
        <div class="col-md-4">
            {% include 'entity/view/partials/actions/small' %}
        </div>
    </section>
    {% for index, child in entity.getChildren('link', 'grouped-text-exercise') %}
        <section class="row">
            <div class="col-xs-1">
                <em>{{ toAlpha(index) }}) </em>
            </div>
            <div class="col-xs-11">
                {% include 'entity/view/default' with {'entity': child, 'parent': entity} only %}
                <hr>
            </div>
        </section>
    {% endfor %}
</article>
<hr>