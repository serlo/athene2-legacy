{{ pageHeader('Event log') }}
<ul>
    {% for event in paginator %}
        <li>{{ eventLog().renderEvent(event) }}</li>
    {% endfor %}
</ul>
{{ paginationControl(paginator, 'Sliding', 'common/paginator', {'route': 'event/history/all'}) }}