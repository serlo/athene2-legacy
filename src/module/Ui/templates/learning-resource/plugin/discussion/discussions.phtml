<?php $form = $this->plugin->getForm(); ?>
<?php $form->setAttribute('action', $this->url('discussion/start',array('on' => $this->entity->getId()))); ?>
<div class="list-group">  
    <?php if(!$this->isArchived): ?>
        <div class="list-group-item">
            <h4><?php echo $this->translate('Start a new discussion'); ?></h4>
            <hr>
            <?php $form->prepare(); ?>
            <?php echo $this->form()->render($form, null); ?>
        </div>
    <?php endif; ?>
    <?php foreach($this->discussions as $discussion): ?>
        <div  class="list-group-item">
            <?php echo $this->partial('discussion/discussion/show', array('discussion' => $discussion, 'user' => $this->user)); ?>
        </div>
    <?php endforeach; ?>
</div>