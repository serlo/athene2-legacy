{% do normalize().headMeta(revision).headTitle(revision) %}
<div class="page-header">
    <div class="pull-right">
        {% include 'page/partials/controls' %}
    </div>
    <h1>{{ revision.getTitle() }}</h1>
    <div class="edit-control hidden">
        {% include 'page/partials/actions' %}
    </div>
</div>