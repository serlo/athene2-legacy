{% set template = include(['subject/taxonomy/entity/' ~ entity.getType().getName(), 'entity/view/default'], ignore_missing = true) %}
{% if template %}
    {{ template }}
{% else %}
    {{ translate('Template for %s not found') | format(entity.getType().getName()) }}
{% endif %}